# Office Add-ins for Word, Excel, PowerPoint

The Glossary add-in will build a Word extension to manage a glossary in a document.

Use npm install to retrieve dependencies and then npm start:

```
npm install
npm start
```
